let animal = ["horse", "tiger", "zebra"];
for (let index = 0; index < animal.length; index++) {
    if (index == 0 ) {
    console.log(`A ${animal[index]} would make a great pet!!`); 
    } else if (index == 1) {
    console.log(`A ${animal[index]} would make a great pet!!`); 
    } else if (index == 2) {
    console.log(`A ${animal[index]} would make a great pet!!`); 
    }
    
}

console.log(
    "Horse is a pet animal. \nTiger is a wild animal. \nZebra has unique colors."
);