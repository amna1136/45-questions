function make_great(megicians: string[]) {
    for (let i = 0; i < megicians.length; i++) {
    megicians[i] = megicians[i] + "the great";}
    }
    // make_great(megicians);
    // modifies the orignal array show_megicians(megicians);
    // show modified names